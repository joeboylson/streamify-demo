import GridItem from "../GridItem";

export default function RevenueDistributionChart() {
  return (
    <GridItem sizeL={5} sizeM={3}>
      - Revenue Distribution: A pie chart that shows the breakdown of revenue
      from different sources (e.g., Subscriptions, Ads).
    </GridItem>
  );
}
